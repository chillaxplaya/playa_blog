+++
# =============================================================================
# Hugo 文章模板 — 默认 archetype
# =============================================================================
# 使用方式：hugo new posts/my-article/index.md
# 每个字段的作用说明详见下方注释。

# -------------------------------- 核心字段 ----------------------------------
# 文章标题（必填）。hugo new 会自动从文件名生成，手动修改即可。
title = 'Posts'

# 发布日期（必填）。格式为 ISO 8601: YYYY-MM-DDTHH:MM:SS+时区偏移
# hugo new 时会自动填入当前时间；可手动改为定时发布或回溯日期。
date = '2026-06-24T08:49:48+08:00'

# 是否为草稿（必填）。draft = true 的文章在生产构建（hugo build）中不会被渲染，
# 但在 hugo server 开发模式下默认可见（加 -D 参数）。
draft = true

# 文章摘要（强烈建议填写）。用于：
#   1. 列表页（blog list）的文章简介卡片
#   2. <meta name="description"> SEO 描述（可被搜索引擎展示）
#   3. Open Graph / Twitter Card 的 og:description
# 建议 120-160 字，用一段话概括文章核心观点。
summary = ''

# 最后修改时间（可选）。
# 用于向读者和搜索引擎标明内容的最近更新日期。不填则默认与 date 一致。
# lastmod = ''

# -------------------------------- 分类体系 ----------------------------------
# 标签（强烈建议填写）。一个文章可以有多个标签。
# 标签是横向的：描述文章涉及的"主题/技术/关键词"。
# 命名规范：小写英文短词 / 中文短词，多词用连字符连接（kebab-case）。
# 示例：
#   tags = ['hugo', 'web-dev', 'tutorial']
#   tags = ['生活', '读书', '随笔']
tags = []

# 分类（可选）。一个文章通常只属于一个分类。
# 分类是纵向的：描述文章的"内容大类/专栏"。
# 建议值：tech / life / reading / project
# categories = ['']

# -------------------------------- SEO 字段 -----------------------------------
# 自定义 URL 别名（可选）。当文章迁移或需要多个 URL 指向同一内容时使用。
# 示例：aliases = ['/old-url/', '/another-path/']
# aliases = []

# Open Graph / Twitter Card 配图（可选）。
# 用于社交媒体分享时的预览图。填 static/ 下的相对路径。
# images = ['/images/og/default.jpg']

# SEO 关键词（可选）。逗号分隔的额外关键词，用于 meta keywords。
# keywords = []

# -------------------------------- 高级字段 -----------------------------------
# 自定义 slug（可选）。覆盖 Hugo 默认从文件名生成的 URL 路径。
# slug = ''

# 是否隐藏文章（可选）。设为 true 时文章不会出现在列表页，
# 但通过直接 URL 仍可访问（与 draft 不同）。
# hidden = false

# 权重（可选）。控制文章排序，数值越大越靠前。通常不设，按日期排序即可。
# weight = 0

# 指定内容类型（可选）。默认从目录推断（如 posts/ → type: posts）。
# type = 'post'

# 指定布局模板（可选）。用于覆盖默认的单页模板。
# layout = ''

# 音频文件路径（可选）。用于播客类文章。
# audio = []

# 系列（可选）。同一系列的文章可以通过此字段关联。
# series = []

# =============================================================================
# 文章正文（Markdown）
# =============================================================================
# 以下是推荐的 Markdown 文章结构，请根据实际需要修改：
#
#   ## 引言 / 背景
#   （简要介绍文章主题、写作动机）
#
#   ## 正文
#   （按照逻辑递进展开，使用二级/三级标题组织段落）
#
#   ## 总结
#   （回顾核心观点，给出结论或行动建议）
#
#   ---
#   （可选）参考文献、延伸阅读链接
# =============================================================================
+++
